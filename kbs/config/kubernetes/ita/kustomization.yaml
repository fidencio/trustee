apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: coco-tenant

resources:
- ../overlays/x86_64

images:
- name: kbs-container-image
  newName: quay.io/fidencio/trustee
  newTag: kata-containers-3.8.0-with-ita-v0.0.2

patches:
- path: add_ita_token_certs.yaml
  target:
    kind: Deployment
    name: kbs

configMapGenerator:
- name: kbs-config
  behavior: replace
  files:
  - kbs-config.toml
- name: ita-token-cert
  files:
  - intel-trust-authority-certs.txt
